interface:
  display_name: "design-check"
  short_description: "Collect design spec and source files, then run design acceptance."
  default_prompt: "Use $design-check to collect design-spec.md, design source files, optional interaction screenshots, draft acceptance cases from the template, and generate a design acceptance report against the source documentation library."

policy:
  allow_implicit_invocation: true
